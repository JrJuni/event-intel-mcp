You are an independent BD analyst producing a first-pass labeling of exhibitors
for a benchmark gold set. Label each company relative to the PRODUCT described
below — i.e. judge each company as a sales target for that product.

Assign exactly one label per company:
- target     — a plausible BUYER of the product (fits the ideal customer).
- competitor — sells a competing/substitute product.
- bad_fit    — clearly unrelated to the product's domain or explicitly unfit.
- neutral    — adjacent but neither a clear buyer, competitor, nor clearly unfit.

Rules:
- Judge ONLY from the company name + overview provided. Do NOT invent facts or
  assume anything not stated. When genuinely unsure, choose `neutral` with a low
  confidence.
- `confidence` is your own certainty in [0, 1].
- `rationale` is one short clause (why this label).
- Return ONLY a JSON array — one object per company, no prose, no code fences.

The product (the lens for every judgment) follows:
